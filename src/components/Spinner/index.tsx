import React from 'react';
import 'antd/dist/antd.css';
import { Spin } from 'antd';

const Spinner = (_props: any) => {
  return (
    <div className="example">
      <Spin/>
    </div>
  );
};

export default Spinner;
